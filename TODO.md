1. - [X] 新增权限系统，修复可能的漏洞
2. - [X] 
部署页面持久化
main的适配器
部署墨狐时先安装uv

- [X] ctr+单机或Ctrl+鼠标框选以多选块，

- [X] 4. 在文件导入的列表左侧用这两个图标，开启或关闭隐藏模式 
- [X] 5. 当工作台主界面位于文件夹中时，新建的项目是以当前文件夹为基准的（自动确定路径），还有新建文件夹和上传项目也是这样。
- [X] 6. 设计一个目录导航，像“test > versino > Getversion”等。
- [X] 7.  完善顶栏的搜索功能，在搜索栏右侧弄一个按键，图标是     这是精确搜索功能，默认模糊搜索，精确搜索要求文件名或文件夹名完全一致，大小写区分开启等，开启后图标颜色变红。
- [X] 8. 搜索栏左侧的加号，点击后弹出一个下拉框选择新建文件，新建模板，新建文件夹，接上对应的逻辑。列表模式下，部署流程的左侧方框的右边，不要紧贴名字，在他俩中间放一个五角星，和卡片布局下，卡片底栏右侧的五角星一样，点击后用黄色填充，对应的部署流程名字显示在左侧星标工作台栏中，星标工作台字样右侧的加号，点击后弹窗，列出所有部署流程，可以选择将哪些设为星标，哪些取消星标，同样给一个搜索栏搜索；风格要符合我的工作台。
- [X] 4. 有关上传项目功能，点击后弹窗，可拖拽导入，可选择导入，白名单中的文件可以一次性导入多个，压缩包，.iso .mcsmod 模板文件一次只能导入一个，但是如果是在某个项目文件夹中导入的，则可以导入多个，全部导入到当前文件夹。当上传的是.toml的模板文件，那就直接让用户指定导入目标，然后塞进工作台首页就好，如果是在一个项目文件夹中导入的，那就是导入到当前文件夹，自动填充导入位置；如果是压缩包，那就解压后连同目录结构，按照用户指定的位置导入，记得拆掉顶层目录，如果导入后的目录超出5层嵌套，那就告诉用户导入失败，如果解压需要密码，那就弹窗向用户索要，.iso同理，.mcsmod是.iso编码改后缀。
- [X] 5. 在快捷栏的新建项目的后面，再插入一个按键，必须！！必须！！！严格按照快捷栏的三个按键的风格和比例——白色/黑色圆角外框，左侧的图标，图标右侧的白色/黑色较大字号的按键名字，下面小的灰色文字说明——图标是这个按键名字是“新建文件”，这里的新建文件只能新建白名单中的文件，禁止新建二进制文件，同样，如果是在首页新建的，要询问文件新建在哪里。说明是“新建用于导入的代码文件”
- [X] 6. 在工作台上双击文件（非二进制，非模板），可以打开Web编辑器进行编辑

- [X] 回收站

- [X] 试运行、调试

- [ ] 现在开发发布项目的按键。
模板流程分为组件、部署、配置、启动、卸载。
webui\frontend\src\pages\Deployment.tsx
在部署管理页面上的“部署实例”“更新实例”“删除实例”三个按键后添加一个按键“部署流”，里面放被发布的部署流程，通过选择卡片来选择要不熟的部署流程，然后进入信息输入页，按部署流程中的要求让用户输入信息，然后进入组件页，在此处选择要安装的组件（仅限组件中开启了用户选择的组件，未开启视为强制安装或检查，这些组件的列表告诉用户）。其他的交互动作之类的，你来设计就好了
MOD\MCStart_部署模版开发文档.md

然后是启动，部署好的模板实例同样以序列号和绝对序列号为标识，实例显示在
webui\frontend\src\pages\Instances.tsx
列表中，选中实例后，按照部署流中提供的启动参数，启动实例，并显示启动结果。

还有配置，
webui\frontend\src\pages\Config.tsx
用户选择实例后可以编辑注册信息，打开配置文件或实例所在目录。注册新实例不支持注册除已支持的实例种类（MaiBot，MoFox Core，Neo MoFox）配置的任何实例。

最后是卸载，
webui\frontend\src\pages\Deployment.tsx
选择删除实例按键，选择对应的实例，用户确认删除后，通过对应部署流程的卸载逻辑，卸载实例。

发布后的模板在工作台上显示一个“已发布”的标签，双击标签弹窗选择是否操作“取消发布，如果取消发布，则收回上述的权限，已部署的实例灰化，不能启动，删除，打开配置。不能再部署新实例。